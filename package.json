{
  "name": "jarvis-content-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Agentic content command center for founder strategy posts.",
  "scripts": {
    "check": "node -c api/generate.js && node -c api/health.js && node -c lib/jarvis-engine.js",
    "dev": "vercel dev",
    "start": "vercel dev"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@vercel/speed-insights": "^2.0.0"
  }
}
